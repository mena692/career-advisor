<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: "Ask a question", required: true, placeholder: "Ask me for advices for your careers" %>
  <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
<% end %>
